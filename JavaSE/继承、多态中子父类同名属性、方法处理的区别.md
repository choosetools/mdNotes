继承、多态中对于子父类同名属性和方法之间处理有些区别

多态性的体现：

```java
Person person = new Student()；
```

可以看成创建的实际上是Person的对象，只不过在调用方法时如果Student对该方法进行了重写，调用的是重写后的方法。

但是，多态性仅仅体现在方法上，对于属性而言，如果Student类中有Person中同名的属性，使用person调用时调用的是依旧是Person类中的属性。

因为实际上创建的是Person的类对象，只有当Person类中存在该属性或者该方法时才会在编译时出现，才可以调用。

